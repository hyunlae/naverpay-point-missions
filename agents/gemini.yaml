interface:
  display_name: "NaverPay Point Missions"
  short_description: "Run NaverPay point missions safely"
  default_prompt: "Discover, review, and run NaverPay point missions from point.pay.naver.com with manual login and verification."
runtime:
  skill_entrypoint: "SKILL.md"
  discover_command: "node scripts/discover_missions.mjs"
  run_command: "node scripts/run_missions.mjs"
safety:
  manual_login_required: true
  manual_verification_required: true
